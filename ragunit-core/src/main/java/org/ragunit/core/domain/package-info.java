/**
 * Domain model for RAGUnit.
 *
 * <p>Contains the immutable value objects that form the ubiquitous language of the library:
 * {@code Question}, {@code Answer}, {@code Document}, {@code Verdict}, and {@code Score}.
 * All types in this package are Java records — they carry data and enforce invariants,
 * but contain no infrastructure or I/O logic.
 *
 * <p>See {@code DOMAIN.md} at the project root for the full glossary.
 */
package org.ragunit.core.domain;
