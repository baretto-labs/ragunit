<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    RAGUnit Checkstyle configuration.
    Goal: enforce readability and Java 17 idioms without being pedantic.
    Any suppression must be commented in the code with the reason.
-->
<module name="Checker">

    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- No tab characters — spaces only -->
    <module name="FileTabCharacter"/>

    <!-- No trailing whitespace -->
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="message" value="Trailing whitespace."/>
    </module>

    <module name="TreeWalker">

        <!-- === Imports === -->
        <module name="AvoidStarImport"/>
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="IllegalImport">
            <!-- Ban sun.* internals — use java.net.http instead -->
            <property name="illegalPkgs" value="sun"/>
        </module>

        <!-- === Naming — must match DOMAIN.md glossary === -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>

        <!-- === Size — readability rule: a class should fit in 5 minutes === -->
        <module name="MethodLength">
            <property name="max" value="20"/>
            <property name="tokens" value="METHOD_DEF, CTOR_DEF"/>
        </module>
        <module name="ParameterNumber">
            <property name="max" value="5"/>
        </module>

        <!-- === Complexity === -->
        <module name="CyclomaticComplexity">
            <property name="max" value="5"/>
        </module>

        <!-- === Code quality === -->
        <module name="EmptyBlock"/>
        <module name="NeedBraces"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="OneStatementPerLine"/>
        <module name="MultipleVariableDeclarations"/>
        <module name="FallThrough"/>
        <module name="MissingSwitchDefault"/>
        <module name="DefaultComesLast"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="StringLiteralEquality"/>
        <module name="EqualsHashCode"/>
        <module name="HiddenField">
            <property name="ignoreSetter" value="true"/>
            <!-- Fluent builders return 'this': treat them as setters -->
            <property name="setterCanReturnItsClass" value="true"/>
            <property name="ignoreConstructorParameter" value="true"/>
        </module>

        <!-- No magic numbers (except -1, 0, 1, 2 which are universally understood) -->
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1, 0, 1, 2"/>
            <property name="ignoreAnnotation" value="true"/>
            <property name="ignoreHashCodeMethod" value="true"/>
        </module>

        <!-- === Javadoc — required on all public types and methods === -->
        <module name="MissingJavadocType">
            <property name="scope" value="public"/>
        </module>
        <module name="MissingJavadocMethod">
            <property name="scope" value="public"/>
            <!-- Records and simple getters do not need Javadoc -->
            <property name="allowMissingPropertyJavadoc" value="true"/>
        </module>

        <!-- === Whitespace (basic consistency) === -->
        <module name="WhitespaceAround">
            <property name="allowEmptyConstructors" value="true"/>
            <property name="allowEmptyMethods" value="true"/>
            <property name="allowEmptyTypes" value="true"/>
        </module>
        <module name="NoWhitespaceBefore"/>
        <module name="GenericWhitespace"/>

    </module>

</module>
